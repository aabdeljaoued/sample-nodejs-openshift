apiVersion: v1
kind: Service
metadata:
  name: {{ include "sample-nodejs-openshift.fullname" . }}
  labels:
    {{- include "sample-nodejs-openshift.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "sample-nodejs-openshift.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
